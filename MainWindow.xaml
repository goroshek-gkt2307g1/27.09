<Window x:Class="_27._09.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_27._09"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="445">
    <Grid>
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Ellipse Name="ellipse" Grid.Row="0" Width="200" Height="200" 
                     HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse.Fill>
                    <RadialGradientBrush GradientOrigin="0.3,0.3">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="Blue" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Window.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <PointAnimation 
                                    Storyboard.TargetName="ellipse"
                                    Storyboard.TargetProperty="Fill.GradientOrigin"
                                    From="0.8,0.2" To="0.2,0.8" 
                                    Duration="0:0:3"
                                    AutoReverse="True" 
                                    RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>

            <Ellipse x:Name="shadow_ellipse" Grid.Row="1" 
                     Width="200" Height="150"
                     HorizontalAlignment="Center"
                     Opacity="0.2" RenderTransformOrigin="0.5,0.5">
                <Ellipse.Fill>
                    <VisualBrush Visual="{Binding ElementName=ellipse}"/>
                </Ellipse.Fill>
                <Ellipse.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="0.8" ScaleY="-0.5"/>
                    </TransformGroup>
                </Ellipse.RenderTransform>
            </Ellipse>
        </Grid>
    </Grid>
</Window>